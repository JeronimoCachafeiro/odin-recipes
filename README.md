# odin-recipes
A simple website that contains recipes. The goal is to practice HTML (boilerplate; elements such as headings, paragraphs, images and lists), Git (Git workflow, atomic commits) and CSS (selectors; properties such as text color, background color, font size, etc.).